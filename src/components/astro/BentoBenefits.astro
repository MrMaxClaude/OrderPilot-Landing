---
interface CostSection {
  label: string;
  headline: string;
  mathRows: { label: string; value: string; highlight: boolean }[];
  summaryLabel: string;
  summaryValue: string;
  sidebar: string;
  sidebarFootnote: string;
}

interface ComparisonSection {
  label: string;
  items: { metric: string; description: string }[];
}

interface CalculatorCta {
  headline: string;
  description: string;
  buttonText: string;
  buttonHref: string;
}

interface WhySection {
  label: string;
  headline: string;
}

interface BentoCard {
  title: string;
  description: string;
  secondDescription?: string;
  badge?: string;
  erps?: string[];
  variant: 'default' | 'standout' | 'wide' | 'compact';
  colSpan: number;
}

interface Props {
  costSection: CostSection;
  before: ComparisonSection;
  after: ComparisonSection;
  calculatorCta: CalculatorCta;
  whySection: WhySection;
  cards: BentoCard[];
}

const { costSection, before, after, calculatorCta, whySection, cards } = Astro.props;
---

<section
  id="benefits"
  class="py-20 lg:py-32 fade-in border-t border-gray-100"
>
  <!-- Section Header -->
  <div class="max-w-6xl mx-auto px-6 mb-12">
    <p class="text-xs font-bold tracking-[0.2em] text-gray-400 uppercase">
      {costSection.label}
    </p>
    <h2 class="text-4xl md:text-5xl lg:text-6xl font-[800] tracking-tight text-text-primary leading-[1.1] mt-4">
      Manual PO processing is{' '}
      <span class="font-serif italic text-rb2-orange">more expensive</span>{' '}
      than you think.
    </h2>
  </div>

  <!-- The Real Cost Stack -->
  <div class="max-w-6xl mx-auto px-6 mb-24 space-y-6">

    <!-- 1. Direct Labor Cost Block (100% Width) -->
    <div class="bg-white rounded-3xl border border-gray-100 p-8 md:p-12 shadow-sm">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <p class="text-xs font-bold tracking-[0.2em] text-gray-400 uppercase mb-6">
            Here's the math
          </p>
          <div class="space-y-5">
            {costSection.mathRows.map((row) => (
              <div class="flex items-baseline justify-between border-b border-gray-50 pb-4">
                <span class="text-gray-600">{row.label}</span>
                <span class={`text-xl font-bold ${row.highlight ? 'text-rb2-orange' : 'text-text-primary'}`}>{row.value}</span>
              </div>
            ))}
            <div class="flex items-baseline justify-between pt-2">
              <span class="text-lg font-bold text-text-primary">{costSection.summaryLabel}</span>
              <span class="text-3xl font-[800] text-text-primary">{costSection.summaryValue}</span>
            </div>
          </div>
        </div>
        <div class="bg-warm-bg-alt rounded-2xl p-8 border border-gray-100">
          <p class="text-lg text-text-secondary leading-relaxed" set:html={costSection.sidebar} />
          <p class="text-xs text-text-muted mt-6">
            {costSection.sidebarFootnote}
          </p>
        </div>
      </div>
    </div>

    <!-- 2. Comparison Cards (50% Width Each) -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Before Column -->
      <div class="bg-white rounded-3xl border border-gray-100 p-8 md:p-10 shadow-sm">
        <p class="text-xs font-bold tracking-[0.2em] text-text-muted uppercase mb-8">
          {before.label}
        </p>
        <div class="space-y-6">
          {before.items.map(item => (
            <div class="flex items-baseline gap-4">
              <span class="text-2xl font-[800] text-gray-400 w-24 flex-shrink-0 text-right whitespace-nowrap">{item.metric}</span>
              <span class="text-sm text-text-secondary leading-tight">{item.description}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- After Column -->
      <div class="bg-gray-900 rounded-3xl p-8 md:p-10 relative overflow-hidden shadow-xl">
        <!-- Subtle background glow -->
        <div class="absolute top-0 right-0 w-64 h-64 bg-rb2-orange/10 blur-[100px] -mr-32 -mt-32"></div>

        <p class="text-xs font-bold tracking-[0.2em] text-rb2-orange uppercase mb-8 relative z-10">
          {after.label}
        </p>
        <div class="space-y-6 relative z-10">
          {after.items.map(item => (
            <div class="flex items-baseline gap-4">
              <span class="text-2xl font-[800] text-rb2-orange w-24 flex-shrink-0 text-right whitespace-nowrap">{item.metric}</span>
              <span class="text-sm text-gray-400 leading-tight">{item.description}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- 3. Calculator CTA (100% Width) -->
    <div class="bg-[#FFF9F5] rounded-3xl border border-rb2-orange/10 p-8 md:p-12 flex flex-col md:flex-row items-center justify-between gap-8 group">
      <div class="max-w-2xl">
        <h3 class="text-2xl md:text-3xl font-[800] text-text-primary tracking-tight mb-4">
          Want <span class="font-serif italic text-rb2-orange">your</span> actual number?
        </h3>
        <p class="text-lg text-text-secondary leading-relaxed">
          {calculatorCta.description}
        </p>
      </div>
      <a
        href={calculatorCta.buttonHref}
        class="w-full md:w-auto inline-flex items-center justify-center px-10 py-5 bg-rb2-orange text-white rounded-2xl font-bold text-lg hover:bg-rb2-orange-hover hover:shadow-2xl hover:shadow-orange-200/50 transition-all group-hover:-translate-y-1"
      >
        {calculatorCta.buttonText}
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      </a>
    </div>
  </div>

  <div class="max-w-6xl mx-auto px-6">
    <div class="h-px bg-gray-100 w-full mb-24"></div>
  </div>

  <!-- Why OrderPilot Heading -->
  <div class="max-w-6xl mx-auto px-6 mb-12">
    <span class="text-xs font-medium uppercase tracking-[0.2em] text-text-muted mb-4 block">{whySection.label}</span>
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-extrabold tracking-tight text-text-primary mb-6">
      Built for procurement teams<br />
      that <span class="font-serif italic text-rb2-orange">can't afford</span> mistakes.
    </h2>
  </div>

  <!-- Bento Grid -->
  <div class="max-w-6xl mx-auto px-6">
    <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
      {cards.map((card) => {
        if (card.variant === 'standout') return (
          <div class={`${card.colSpan === 3 ? 'md:col-span-3' : 'md:col-span-2'} bg-rb2-orange text-white rounded-2xl p-8 hover:translate-y-[-2px] hover:shadow-lg transition-all duration-300 relative overflow-hidden flex flex-col justify-between`}>
            <!-- Zap icon (large decorative) -->
            <svg xmlns="http://www.w3.org/2000/svg" width="200" height="200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="absolute -bottom-10 -right-10 text-white opacity-10"><path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z"/></svg>
            <div class="relative z-10">
              <h3 class="text-xl md:text-2xl font-bold mb-4">{card.title}</h3>
              <p class="text-white/80 mb-6">{card.description}</p>
            </div>
            {card.badge && (
              <div class="relative z-10">
                <div class="inline-flex items-center gap-2 bg-white/20 backdrop-blur-sm rounded-full px-4 py-2 text-xs font-bold uppercase tracking-widest">
                  {card.badge}
                </div>
              </div>
            )}
          </div>
        );

        if (card.variant === 'wide') return (
          <div class={`${card.colSpan === 3 ? 'md:col-span-3' : 'md:col-span-2'} bg-card-white rounded-2xl p-8 border border-gray-100 hover:translate-y-[-2px] hover:shadow-lg transition-all duration-300`}>
            <!-- Layers icon -->
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-rb2-orange mb-6"><path d="M12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83z"/><path d="m2 12 8.58 3.91a2 2 0 0 0 1.66 0L20.83 12"/><path d="m2 17 8.58 3.91a2 2 0 0 0 1.66 0L20.83 17"/></svg>
            <h3 class="text-xl md:text-2xl font-bold text-text-primary mb-4">{card.title}</h3>
            <p class="text-text-secondary mb-8">{card.description}</p>
            {card.erps && (
              <div class="flex flex-wrap gap-3">
                {card.erps.map((erp) => (
                  <span class="bg-warm-bg-alt rounded-lg px-3 py-1 text-sm font-medium text-text-secondary">
                    {erp}
                  </span>
                ))}
              </div>
            )}
          </div>
        );

        if (card.variant === 'compact') return (
          <div class={`${card.colSpan === 3 ? 'md:col-span-3' : 'md:col-span-2'} bg-card-white rounded-2xl p-8 border border-gray-100 hover:translate-y-[-2px] hover:shadow-lg transition-all duration-300 flex flex-col justify-center group`}>
            <h3 class="text-xl font-bold text-text-primary mb-2">{card.title}</h3>
            <p class="text-text-secondary text-sm">{card.description}</p>
          </div>
        );

        /* default variant */
        return (
          <div class={`${card.colSpan === 3 ? 'md:col-span-3' : 'md:col-span-2'} bg-card-white rounded-2xl p-8 border border-gray-100 hover:translate-y-[-2px] hover:shadow-lg transition-all duration-300 flex flex-col justify-between`}>
            <div>
              <!-- ShieldCheck icon -->
              <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-rb2-orange mb-6"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/><path d="m9 12 2 2 4-4"/></svg>
              <h3 class="text-xl md:text-2xl font-bold text-text-primary mb-4">{card.title}</h3>
              <p class="text-text-secondary mb-6">{card.description}</p>
              {card.secondDescription && <p class="text-text-secondary mb-8">{card.secondDescription}</p>}
            </div>
            {card.badge && (
              <div class="inline-flex items-center gap-2 bg-rb2-orange/5 rounded-full px-4 py-2 text-xs font-bold uppercase tracking-widest text-rb2-orange">
                <span class="w-2 h-2 bg-rb2-orange rounded-full animate-pulse"></span>
                {card.badge}
              </div>
            )}
          </div>
        );
      })}
    </div>
  </div>
</section>
